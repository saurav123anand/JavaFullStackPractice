-- to get the current time stamp
-- select CURRENT_TIMESTAMP

-- LIMIT Clause
-- select * from employee limit 3; -- this will fetch only 3 rows from top

-- order by 
/* the SQL ORDER BY clause is used to sort the data in ascending or descending order, 
based on one or more columns. Some databases sort the query results in an ascending order by default. */
-- select * from employee order by salary;
-- select * from employee order by salary desc;
-- select * from employee order by name,salary;
select * from employee;
